export const POOL_REGISTRY_SEED = Buffer.from("pool_registry");
export const PAIR_SEED = Buffer.from("pair");
export const ORACLE_PRICE_HISTORY_SEED = Buffer.from("oracle_price_history");
export const LIQUIDITY_ACCOUNT_SEED = Buffer.from("liquidity_account");
